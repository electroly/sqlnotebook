This is a snapshot of GNU Libmicrohttpd.

The code in mhd\ was downloaded from: http://ftp.gnu.org/gnu/libmicrohttpd/libmicrohttpd-0.9.50.tar.gz

Then it was processed using process-mhd-bundle.ps1 to produce the code snapshot here.
